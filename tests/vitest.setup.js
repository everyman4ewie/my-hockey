/**
 * Глобальная среда по умолчанию — node (API, shared).
 * Для RTL + jsdom: `import '@testing-library/jest-dom/vitest'` в этом файле
 * и в тесте компонента — `/** @vitest-environment jsdom *\/`
 */
